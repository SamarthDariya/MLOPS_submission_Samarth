# Flask settings
SECRET_KEY=  # Required: Generate a secure random key
DEBUG=False

# Database settings
DATABASE_URL=sqlite:///mlops_platform.db

# JWT settings
JWT_SECRET_KEY=  # Required: Generate a secure random key for JWT tokens
JWT_COOKIE_SECURE=False  # Set to True in production with HTTPS

# Redis settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Deployment settings
MAX_QUEUE_SIZE=100
DEFAULT_DEPLOYMENT_PRIORITY=1
MAX_DEPLOYMENT_PRIORITY=5

# Resource limits
MAX_RAM_GB_PER_DEPLOYMENT=32
MAX_CPU_CORES_PER_DEPLOYMENT=8
MAX_GPU_COUNT_PER_DEPLOYMENT=4

# Security settings
PASSWORD_MIN_LENGTH=8
INVITE_CODE_EXPIRY_HOURS=24

# Scheduling settings
SCHEDULER_INTERVAL_SECONDS=10
MAX_CONCURRENT_DEPLOYMENTS_PER_CLUSTER=10 